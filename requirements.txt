# ============================================================================
# Proyecto MCDI501 — Análisis Estadístico de la Calidad del Aire en Santiago
# Grupo 3 · Magíster en Ciencia de Datos e IA · Universidad Andrés Bello
# ----------------------------------------------------------------------------
# Dependencias directas del proyecto
# Python validado: 3.12.10 · Kernel: .venv
#
# Criterio de trazabilidad:
# - Las versiones exactas del núcleo científico, visualización y modelamiento
#   fueron verificadas en el entorno activo mediante `pip freeze`.
# - Este archivo contiene solo dependencias directas y relevantes. Las
#   dependencias transitivas son resueltas automáticamente por pip.
# - nbconvert se limita a la rama 7.x utilizada por el flujo de ejecución y
#   renderizado, evitando cambios incompatibles de una versión mayor futura.
# ============================================================================

# Núcleo científico
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0

# Visualización
matplotlib==3.11.0
seaborn==0.13.2

# Modelamiento predictivo e inferencia — Sumativa 3
# APIs utilizadas: Pipeline, ColumnTransformer, OneHotEncoder, StandardScaler,
# LogisticRegression, BayesianRidge, SimpleImputer, IterativeImputer,
# métricas de clasificación, calibration_curve y GLM Binomial.
scikit-learn==1.9.0
statsmodels==0.14.6

# Ejecución, kernel y renderizado de notebooks
ipykernel==6.29.5
notebook==7.6.0
nbconvert>=7.16,<8.0

# ----------------------------------------------------------------------------
# Instalación recomendada en Windows PowerShell:
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
# Verificación de versiones principales:
#   python -c "import sys, numpy, pandas, scipy, matplotlib, seaborn, sklearn, statsmodels; print(f'Python={sys.version.split()[0]} | NumPy={numpy.__version__} | pandas={pandas.__version__} | SciPy={scipy.__version__} | matplotlib={matplotlib.__version__} | seaborn={seaborn.__version__} | scikit-learn={sklearn.__version__} | statsmodels={statsmodels.__version__}')"
#
# Ejecución oficial S3:
#   Remove-Item Env:S3_FAST -ErrorAction SilentlyContinue
#   jupyter nbconvert --to notebook --execute --inplace notebooks/03_Modelamiento_predictivo_binario_mala_calidad_mp25.ipynb
#
# Verificación rápida S3:
#   $env:S3_FAST = "1"
#   jupyter nbconvert --to notebook --execute --inplace notebooks/03_Modelamiento_predictivo_binario_mala_calidad_mp25.ipynb
#   Remove-Item Env:S3_FAST
# ----------------------------------------------------------------------------
